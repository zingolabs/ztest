//! Test inventory (macro-registered decls) — orchestrator contract.

pub use crate::inventory::qos_iter;
pub use crate::inventory::{
    DevImageEntry, QosEntry, SeedEntry, SeedPayload, SyncTestEntry, TestDepEntry,
};
